// Import http and app
const http = require('http');
const app = require('../index');

// Use port 3001 unless there exist a preconfigured port
const port = parseInt(process.env.PORT, 10) || 3001;
app.set('port', port);

const server = http.createServer(app);
server.listen(port); // Listen and accept connections from server
