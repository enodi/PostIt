import http from 'http';

import app from '../index';

// Use port 8888 unless there exist a preconfigured port
const port = parseInt(process.env.PORT, 10) || 8888;
app.set('port', port);

const server = http.createServer(app);
server.listen(port); // Listen and accept connections from server
