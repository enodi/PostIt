engines:
  duplication:
    enabled: true
    checks:
      Similar code:
        enabled: false
    config:
      languages:
        - javascript
    exclude_paths:
    - node_modules/
    - template/
    - coverage/
    ratings:
      paths: 
      - "server/**/*.js"

